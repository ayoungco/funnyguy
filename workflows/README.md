# Workflows

Drop ComfyUI workflow JSON exports here (Workflow → Export in the ComfyUI
UI). None checked in yet — start from the default SDXL txt2img or
img2img/ControlNet workflow, point the checkpoint loader at
`../../stable-diffusion-xl-base-1.0`, and save the export back into this
folder once it's dialed in.
