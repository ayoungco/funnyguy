# Scripts

Pipeline helpers go here — e.g. a batch-generation script that reads a
prompt template from `../prompts/`, submits it to a running ComfyUI
instance via its HTTP API, and drops results in `../output/`. Nothing
built yet; add scripts as the pipeline takes shape.
